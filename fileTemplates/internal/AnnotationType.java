#parse("File Header.java")
#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end

/**
 * ????
 *
 * @version ${DS}Id:${DS}
 * @author ${USER} (c) ${DAY} ${MONTH_NAME_SHORT}. ${YEAR}, Sogeti B.V.
 */
public @interface ${NAME} {
}
